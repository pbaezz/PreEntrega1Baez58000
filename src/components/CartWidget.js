import cart from "../components/img/cart-white.svg";
const CartWidget = () =>{
    return (
        <div>
            <img src={cart} alt="cart.svg"/>0
        </div>
    );
}

export default CartWidget;